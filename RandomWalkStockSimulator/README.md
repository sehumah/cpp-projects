# Random Walk Stock Simulator

This project simulates a stock price path using random numbers.
Data visualization is currently not implemented. A GUI/plotting library will be implemented later with a line graph showing the stock price trends over the whole year.

##

Compile the program with:

```bash
g++ main.cpp -o a.out
```

and run the generated executable file with:

```bash
./a.out
```
